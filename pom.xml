<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Spring Boot parent bumped 3.4.1 -> 3.5.16 on 2026-08-28.

       WHY: the Trivy image scan failed the build on three fixable HIGH
       CVEs in Spring Framework 6.2.x shipped inside app.jar
       (CVE-2026-41842 DoS via versioned static resources,
        CVE-2026-41845 XSS via JavaScriptUtils,
        CVE-2026-41850 algorithmic DoS via SpEL). All three are fixed in
       Spring Framework 6.2.19.

       WHY THE PARENT AND NOT A PROPERTY OVERRIDE: the 3.4.x line ended at
       3.4.13 (Dec 2025) and never received the June 2026 security
       releases. Pinning <spring-framework.version> on an EOL 3.4.1 parent
       would fix these three CVEs while leaving Tomcat, Jackson, Logback and
       Reactor at their 2024 versions. 3.5.16 (Jun 2026) is a supported BOM
       that manages 6.2.19 together with current versions of the rest. -->
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>com.example</groupId>
  <artifactId>app</artifactId>
  <version>1.0.0</version>
  <name>health-check-service</name>
  <description>Tiny Spring Boot health check service exposing /health and /version</description>

  <properties>
    <java.version>17</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <checkstyle.plugin.version>3.6.0</checkstyle.plugin.version>
    <spotbugs.plugin.version>4.8.6.6</spotbugs.plugin.version>
    <jacoco.plugin.version>0.8.12</jacoco.plugin.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>app</finalName>
    <plugins>

      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>

      <!-- Checkstyle: style gate, fails the build on violations. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-checkstyle-plugin</artifactId>
        <version>${checkstyle.plugin.version}</version>
        <configuration>
          <configLocation>checkstyle.xml</configLocation>
          <consoleOutput>true</consoleOutput>
          <failOnViolation>true</failOnViolation>
          <violationSeverity>warning</violationSeverity>
          <includeTestSourceDirectory>false</includeTestSourceDirectory>
        </configuration>
      </plugin>

      <!-- SpotBugs: static analysis for bug patterns. -->
      <plugin>
        <groupId>com.github.spotbugs</groupId>
        <artifactId>spotbugs-maven-plugin</artifactId>
        <version>${spotbugs.plugin.version}</version>
        <configuration>
          <effort>Max</effort>
          <threshold>Medium</threshold>
          <failOnError>true</failOnError>
        </configuration>
      </plugin>

      <!-- JaCoCo: coverage measurement with an enforced minimum. -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.plugin.version}</version>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
          <execution>
            <id>coverage-gate</id>
            <phase>verify</phase>
            <goals>
              <goal>check</goal>
            </goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.70</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- NOTE: the OWASP dependency-check-maven plugin was REMOVED on
           2026-08-28 at the user's request. The unkeyed NVD feed download
           could not complete inside the CI runner's limits and blocked every
           deploy. Dependency-level CVE scanning is currently NOT performed.
           Container image scanning (Trivy, HIGH/CRITICAL, fixable-only)
           still runs in the build_push pipeline stage.
           To restore: re-add the plugin with failBuildOnCVSS=7 and provide an
           NVD_API_KEY repo secret, ideally from a scheduled workflow rather
           than the deploy critical path. -->

    </plugins>
  </build>
</project>
